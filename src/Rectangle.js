class Rectangle {
  // <rect x="50" y="20" width="150" height="150">
  constructor(atomId, width, height, x, y) {
    this.atomId = atomId;
    this.height = height;
    this.width = width;
    this.x = x;
    this.y = y;
  }

  // static createRectangle()
}

export default Rectangle;
